<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below:</p>
      <ul>
        <%= for {attr, message} <- f.errors do %>
          <li><%= humanize(attr) %> <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :tournamentId, "Tournamentid", class: "control-label" %>
    <%= number_input f, :tournamentId, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :affiliaffiliationSchoolId, "Affiliaffiliationschoolid", class: "control-label" %>
    <%= number_input f, :affiliaffiliationSchoolId, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :userId, "Userid", class: "control-label" %>
    <%= number_input f, :userId, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
